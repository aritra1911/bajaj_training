/*
 * Arithmetic operators:
 *  +   -   *   /   **  %   ++  --
 *
 *  ==      checks the value, not the type
 *  ===     checks the type as well
 *  typeof  check type of a literal
 */

a = ["Hello", "Hi", 4];
console.log(a instanceof Array);